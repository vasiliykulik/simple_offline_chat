/*
package ua.goit.offline.chat.myvalidation;

import org.springframework.util.StringUtils;
import ua.goit.offline.chat.validation.Validator;

*/
/**
 * Created by Молния on 20.02.2017.
 *//*

public class MyValidationService implements Validator {

    private static final String HTTP_PATTERN = "https?://[a-zA-Z0-9/.-/w]*";


    @Override
    public boolean isValid(String validate) {
        return !StringUtils.isEmpty(validate) && !validate.matches(HTTP_PATTERN);
    }
}
*/
